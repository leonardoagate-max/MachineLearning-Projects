# CV — "Projects" entries

## Recommended: pick 2–3 of these strong, distinct entries

**Image Classification with Transfer Learning** — Built a flower image classifier two ways, a custom CNN and
a DenseNet121 model pretrained on ImageNet, comparing them with confusion matrices and per-class F1 (~98%
accuracy). TensorFlow/Keras. [GitHub]

**Web Scraping Pipeline** — Built a scraper with requests and BeautifulSoup that extracts structured product
data from an e-commerce site, aggregates search results into a pandas DataFrame and exports filtered data to
Excel. [GitHub]

**Tabular ML — Model Comparison & Tuning** — Compared Random Forest, Gradient Boosting and Logistic
Regression on a census-income classification task with preprocessing pipelines and GridSearchCV, and built a
feed-forward neural network tuned with Keras Tuner on the same data. scikit-learn, TensorFlow/Keras. [GitHub]

## Single combined entry (if you want just one line)

**Machine Learning Portfolio** — Six projects spanning web scraping, classical ML (Random Forest, Gradient
Boosting, Logistic Regression with GridSearchCV), neural networks with Keras Tuner, CNNs, DenseNet121
transfer learning and a convolutional autoencoder. Python, scikit-learn, TensorFlow/Keras. [GitHub]

---

## LaTeX (matching your current Projects format)

\textbf{Image Classification with Transfer Learning}
Built a flower image classifier two ways — a custom CNN and a DenseNet121 model pretrained on ImageNet —
comparing them with confusion matrices and per-class F1 (~98\% accuracy) in TensorFlow/Keras.

\textbf{Web Scraping Pipeline}
Built a scraper with requests and BeautifulSoup extracting structured product data from an e-commerce site,
aggregating search results into a pandas DataFrame and exporting filtered data to Excel.

\textbf{Tabular ML — Model Comparison \& Tuning}
Compared Random Forest, Gradient Boosting and Logistic Regression on a census-income task with preprocessing
pipelines and GridSearchCV, and built a feed-forward neural network tuned with Keras Tuner on the same data.

---

## Skills to add / upgrade in the Skills section

- Under **Programming**: add TensorFlow/Keras, scikit-learn, BeautifulSoup
- Under **Data Science**: replace "Machine learning basics" with:
  Machine learning (Random Forest, Gradient Boosting, Logistic Regression), deep learning (CNN, transfer
  learning, autoencoders), hyperparameter tuning (GridSearchCV, Keras Tuner), web scraping, dimensionality
  reduction, MapReduce/Hadoop
